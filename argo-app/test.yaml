apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: b2bpoke
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/esakarya/test
    chart: odoo-18
    #targetRevision: ""
    helm:
      values: |
        app_name: b2bpoke
        host: b2bpoke.kobimate.com
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    #syncOptions:
    #  - CreateNamespace=true